apiVersion: v1
kind: ConfigMap
metadata:
  name: identity-campaign-config
data:
  rabbitmq-host: "rabbitmq"
  rabbitmq-port: "5672"
  jwt-issuer: "IdentityCampaign.Api"
  jwt-audience: "IdentityCampaign.Client"